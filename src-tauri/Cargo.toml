[package]
name = "video_forge_desktop"
version = "1.4.0"
description = "VideoForge desktop wrapper"
authors = ["VideoForge"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
base64 = "0.22"
rfd = "0.15"

[package.metadata]

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
